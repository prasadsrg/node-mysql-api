/dashboard/{category}:
  get:
    tags:
      - Report
    summary: "to get graph data"
    description:
      DashBoard data.
    parameters:
      - name: category
        in: path
        required: true
        type: string
    responses:
        200:
            description: "status:1 is success; status: 0 is failure;"
        400:
            description: "Invalid Data"
        500:
            description: "Internal server error"
