/auth:
  get:
    summary: Forgot password sent otp
    tags:
      - Access
    parameters:
      - name: vid
        example: DFF
        in: query
        required: true
        type: string
      - name: userid
        example: "123456789"
        in: query
        required: true
        type: string

    responses:
      200:
        description: A single user item
